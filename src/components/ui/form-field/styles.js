import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginBottom: 15
    },
    label: {
        marginBottom: 3,
    },
    input: {
        height: 40,
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
        width: '100%'
    },
    error: {
        color: "red",
    }
});

export default styles;